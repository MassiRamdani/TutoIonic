import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.techiediaries.jsjargon',
  appName: 'jsjargon',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
